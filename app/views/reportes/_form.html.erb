<div class="span8">
  <%= form_for(@reporte) do |f| %>
  <% if @reporte.errors.any? %>
      <div class="alert alert-danger alert-dismissable">
        <i class="fa fa-times"></i>
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
        <b><%= pluralize(@van.errors.count, "error") %>:</b>
        <ul>
          <% @reporte.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
  <% end %>

  <%if params[:mensaje]%>
    <div class="alert alert-danger alert-dismissable">
      <i class="fa fa-times"></i>
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
        <%= params[:mensaje]%>
    </div>
  <%end%>
<h4 style="padding-left:20px">Tu opini&oacute;n es muy importante para nosotros, escr&iacute;benos tus sugerencias o comentarios del servicio.</h4>

    <%= f.text_area :descripcion, :class=>"span12", :rows=>10 %>
                </div> 
                <br>
                <div class="row-fluid">
                <div class="span8">
                    <br>
    
    <%= f.submit 'Enviar', :class => 'btn btn-large btn-primary btn-flat pull-right' %>
    <%= link_to 'Regresar', dashboard_path, class:"btn btn-large btn-flat " %>
                </div>
                </div>
<% end %>