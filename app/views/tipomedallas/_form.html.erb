<%= form_for(@tipomedalla) do |f| %>
  <% if @tipomedalla.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipomedalla.errors.count, "error") %> prohibited this tipomedalla from being saved:</h2>

      <ul>
      <% @tipomedalla.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
