<div class="box">
    <div class="box-header">
        <i class="fa fa-trophy"></i>
        <h3 class="box-title">Últimas Medallas</h3>
    </div>
    <div class="side-wall">
        <% @muro.each do |medalla| %>
            <img src="/assets/<%= medalla.imagen%>" class="img-circle" alt="<%= medalla.nombre%>" /></br>
            <b><%= medalla.nombre%><b>
        <% end %>
    </div>
    <hr></hr>
    <h4 class="text-center"><a href="/clientes/<%= @cliente.id %>/muro">Ver todas mis medallas</a></h4>
    </br>
</div>