<%= form_for(@nivel) do |f| %>
  <% if @nivel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nivel.errors.count, "error") %> prohibited this nivel from being saved:</h2>

      <ul>
      <% @nivel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :rangomaximo %><br />
    <%= f.number_field :rangomaximo %>
  </div>
  <div class="field">
    <%= f.label :estatus %><br />
    <%= f.check_box :estatus %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
