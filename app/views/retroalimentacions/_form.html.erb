<%= form_for(@retroalimentacion) do |f| %>
  <% if @retroalimentacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@retroalimentacion.errors.count, "error") %> prohibited this retroalimentacion from being saved:</h2>

      <ul>
      <% @retroalimentacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reservacion_id %><br />
    <%= f.number_field :reservacion_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
